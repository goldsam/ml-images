# NOTE: torch is installed in the Dockerfile from the PyTorch CUDA index and
# pinned via /opt/torch-constraints.txt. Do not add torch/nvidia-* here.

# Core scientific computing
numpy>=2.5
pandas>=3.0
scipy>=1.18
scikit-learn>=1.9
pydantic>=2.13
typer>=0.27
# RL frameworks (depend on torch, satisfied by the layer above)
gymnasium>=1.3
stable-baselines3>=2.9
# Lightweight experiment tracking
mlflow-skinny>=3.16
